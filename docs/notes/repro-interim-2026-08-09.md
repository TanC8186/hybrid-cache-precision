# S-formal 独立复现中间审计（2026-08-09，R60 完成 / SG 进行中）

## 预注册判据

- 重叠 cell mean goodput 对称相对差 ≤ 10%；
- 可持续边界逐格精确一致；
- 配对 goodput Δ（bf16−fp32）方向与 CI 含 0 性一致。

## R60 复现（30/30 完成，审计通过）

- 完整性：30/30 completed_validated、sidecar/分母/到达窗口（≤0.01%）/SLO 键全过，
  日志硬证据齐全；
- 边界对比（原始 vs 复现）：

| allocation | 250 | 500 | 1000 | 2000/3000 |
|---|---:|---:|---:|---:|
| int4 原始 | 30 | 35 | **35** | 40 |
| int4 复现 | 30 | 35 | **40** | 40 |
| int4_statebf16 原始/复现 | 30 | 35 | 40 | 40 |

  差异 1/10：int4@1000ms 原始 35、复现 40 → “bf16 把 1000ms 边界从 35 提到 40”
  这一具体表述**不复现**（复现中 int4 本身也到 40）。

- 重叠 mean goodput：50 格中 48 格 ≤10%；2 格超差（int4 r40 250ms 0.261→0.313
  相对 18.3%；500ms 0.729→0.810 相对 10.6%，均为过载低均值格）。
- 配对 Δ（显著格方向/量级一致）：r40 250ms +0.334→+0.304、500ms +0.215→+0.138；
  r45 2000/3000ms +0.324/+0.367→+0.338/+0.372；r50 2000/3000ms +0.041/+0.072→
  +0.041/+0.072。微小效应格 2 处 CI 符号翻转（r40 1000ms、r50 250ms），量级
  ≤0.02，不影响实质结论。

## 中间判定

`PARTIALLY_REPRODUCIBLE`：核心 serving 收益（过载区 paired goodput 增益）复现，
但 1000ms 边界单格不一致 + 2/50 低均值格超差。结论：

- 论文不得把“Random60 1000ms 边界 35→40”写成稳定 headline；
- 可写“Random60 过载区（r40 250/500ms、r45、r50 2000/3000ms）bf16 state 的
  paired goodput 增益在独立复现中重现”；
- claim #5 在 SG 复现与全量判定前保持 ANALYZED。
